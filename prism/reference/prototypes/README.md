# Frozen prototypes

Verbatim copies of the validated handoff prototypes (prism_handoff,
Jun 2026). These are the EQUIVALENCE BASELINE: the package's refactored
modules must produce byte-identical outputs to these on the reference
input. Never edit them — they exist so the test suite can prove the
refactor preserved the math.
