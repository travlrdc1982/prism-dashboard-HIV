node_modules/
dist/

# Local data — never committed (top-level data/ only, not src/data)
/data/
*.sav

# Python
__pycache__/
*.pyc
.venv/
venv/

# Topline pipeline build artifacts — regenerated every refresh.py run
pipeline/topline/dashboard.json
pipeline/topline/results_long.csv
pipeline/topline/dashboard.html
prism/prism_platform.egg-info/

# PRISM preflight outputs (weighted .sav + diagnostics) — regenerated
# per run; the weighted .sav is the input to refresh.py, not an artifact
# to ship.
prism/out/
